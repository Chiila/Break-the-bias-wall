# Break-the-bias-wall
Teams treat bias as a “bug” by identifying biased statements in a “corrupted code” paragraph using a code editor-style setup. They highlight the issues and rewrite them into fair, inclusive statements, then input their answers to unlock a reward—symbolizing the removal of bias.
